@prompt: new_review_prompt

# Task
新規コードのレビューを実施せよ。

---

# 実行手順

## Step1: コード全体の把握
- 処理の目的・責務を把握する
- 言語・フレームワーク・外部依存を確認する
- データフロー（入力 → 処理 → 出力）を追う

## Step2: リスク検出
ロードされた Agent・Skill に基づき以下を分析する。

優先順位：
1. 本番障害リスク（P0・P1）
2. セキュリティリスク
3. 性能リスク
4. 回帰・保守リスク（P2・P3）

## Step3: 影響範囲分析
各リスクについて：
- どの処理・テーブル・サービスに影響するか
- 高負荷時・本番環境での具体的な障害シナリオ
- 影響が外部（クライアント・他サービス）に及ぶか

## Step4: テスト推奨
検出したリスクに対するテストケースを提示する。

## Step5: 改善版コードの生成（最終出力）

レビュー結果の最後に、必ず**完全な改善版コード**を出力すること。

### 5-1. リスク修正
Step2〜3 で検出した全ての P0・P1・P2 を修正したコードにすること。
修正箇所が分かるよう、各修正に対して短いコメントを付けること。

```java
// [Fixed P0] @Transactional を追加（トランザクション不整合リスクを解消）
@Transactional
public void placeOrder(...) { ... }
```

### 5-2. クラス分割の判断

以下の基準でクラスが大きすぎるかを判定すること：

| 基準 | 閾値 |
|---|---|
| 行数 | 300行を超える |
| メソッド数 | 15個を超える |
| 責務の数 | 2つ以上の異なる関心事を持つ |

分割が必要と判断した場合：
- 責務ごとに分割したクラス名を提案する
- 各クラスの役割を1行で説明する
- 分割後の呼び出し関係を示す

分割が不要な場合：
- 「分割不要：現状の行数・責務で問題なし」と明記する

### 5-3. フォルダ構成の提案

分割後、機能が類似するクラスは同一フォルダに配置すること。

判断基準：
- 同じドメイン（例：Order関連）→ 同一フォルダ
- 同じレイヤー（例：Repository層）→ 同一フォルダ
- 呼び出し関係が強いクラス → 同一フォルダ

以下の形式で出力すること：

```
src/main/java/com/example/order/
├── OrderService.java          ← 注文の登録・キャンセル
├── OrderQueryService.java     ← 注文の検索・一覧取得（分割後新設）
├── OrderValidator.java        ← 入力検証（分割後新設）
└── OrderRepository.java       ← DB操作（既存）
```

### 5-4. 出力フォーマット

最終出力は以下の構成にすること：

```
## 改善版コード

### フォルダ構成
（5-3 の形式で出力）

### [ファイル名1]
```java
（完全なコード。省略しない）
```

### [ファイル名2]
```java
（完全なコード。省略しない）
```

### 分割理由
（なぜこの分割にしたか、2〜3文で説明）
```

## Step6: 自己検証ループ（出力前に必ず実行）

改善版コードを出力する**前に**、以下を自分自身で検証すること。
検証は声に出さず内部で行い、修正が必要なら直してから最終出力すること。

### 6-1. コンパイル可能性チェック
- import 文は全て揃っているか
- 括弧・セミコロンの対応は正しいか
- 存在しないメソッド・クラスを呼んでいないか
- 型の不一致がないか

### 6-2. 低レベルミスチェック
以下を1つずつ確認し、該当があれば修正すること：
- [ ] 未使用の変数・import が残っていないか
- [ ] null チェックが必要な箇所に抜けがないか
- [ ] 例外を握りつぶしていないか（catch内が空でないか）
- [ ] マジックナンバーがそのまま残っていないか
- [ ] 命名が一貫しているか（同じ概念に違う名前を使っていないか）
- [ ] 重複コードが新たに生まれていないか
- [ ] ログ出力の粒度が適切か（過剰・不足）

### 6-3. 意図保持チェック
- 元のコードが持っていた正常な振る舞いを壊していないか
- Step2で検出した修正対象以外の挙動を変えていないか
- 修正によって新しいリスクを生んでいないか（再帰的に確認）

### 6-4. 再検証
6-1〜6-3 で1つでも修正した場合、修正後のコードに対して
再度 6-1〜6-3 を実行すること。問題がなくなるまで繰り返す。
最大3回まで繰り返し、それでも解決しない場合は
「未解決の懸念事項」として総評に明記すること。

---

# 禁止事項
- コードスタイルのみの指摘
- 「改善できます」だけの曖曖表現
- 根拠のない推測による指摘
- 影響範囲の未記載
- 改善版コードの省略・一部のみの出力（"..." で省略しない）
- 分割の必要性を判断せずに必ず分割する、または必ず分割しないという固定判断
- 機能が異なるクラスを同一フォルダに配置する提案
- 自己検証（Step6）を行わずにコードを出力すること
- 未使用import・空のcatch・マジックナンバーを残したまま出力すること
- 元の正常な振る舞いを変えてしまう修正
- 「コードが大きくなるため段階的に対応する」等の理由で
  改善版コードの生成自体を回避・延期すること
- 「まずP0/P1のみ修正し、その後対応する」という方針提示で
  Step5の出力を終えること（方針ではなくコードそのものを出力する）
- 改善版コードの生成が困難であることを理由に
  生成をスキップして総評だけで終えること

## Step5 は方針提案では完了しない

Step5 の目的はコードを出力することであり、
「どう直すべきか」を説明することではない。
以下は全て不十分な出力として扱う：

  NG: 「P0/P1から段階的に修正するのが妥当です」
  OK: P0/P1を全て修正した完全なコードをこの場で出力する

  NG: 「改善版は大きくなるため別途対応します」
  OK: 大きくなっても全文をこの場で出力する
      （ファイル分割で対応し、省略はしない）

コードが長大になる場合は、5-2のクラス分割で対応し、
分割した各ファイルを Step5 の出力としてすべて提示すること。
「後で」「次回」「別途」という言葉で
コード生成を終わらせることは禁止する。

---

# 出力
ロードされた Template の形式に従い、Step1〜4 の分析結果を出力した後、
Step5〜6 を内部で実行し、検証済みの改善版コードを追加で出力すること。
Step6 で発見した修正点があれば、Step5 の出力に反映済みの状態で出力すること
（検証過程そのものは出力に含めない。最終的な完成コードのみを出力する）。
