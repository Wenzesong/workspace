@prompt: new_review_prompt

# Task
新規コードのレビューを実施せよ。

---

# 実行手順

## Step1: コード全体の把握
- 処理の目的・責務を把握する
- 言語・フレームワーク・外部依存を確認する
- データフロー（入力 → 処理 → 出力）を追う

## Step2: リスク検出
ロードされた Agent・Skill に基づき以下を分析する。

優先順位：
1. 本番障害リスク（P0・P1）
2. セキュリティリスク
3. 性能リスク
4. 回帰・保守リスク（P2・P3）

## Step3: 影響範囲分析
各リスクについて：
- どの処理・テーブル・サービスに影響するか
- 高負荷時・本番環境での具体的な障害シナリオ
- 影響が外部（クライアント・他サービス）に及ぶか

## Step4: テスト推奨
検出したリスクに対するテストケースを提示する。

## Step5: 改善版コードの生成（最終出力・最重要）

レビュー結果の最後に、必ず**完全な改善版コード**を出力すること。
これは本タスクの最終成果物であり、省略は一切認められない。

### 5-1. リスク修正
Step2〜3 で検出した全ての P0・P1・P2 を修正したコードにすること。
修正箇所には短いコメントを付けること。

```java
// [Fixed P0] @Transactional を追加（トランザクション不整合リスクを解消）
@Transactional
public void placeOrder(...) { ... }
```

修正時、以下の低レベルミスを作らないよう注意する（チェックリストではなく、
コードを書く上での当然の前提として扱うこと）：
未使用import・空のcatch・マジックナンバー・命名の不一致・nullチェック漏れ。

### 5-2. クラス分割の判断

以下の基準でクラスが大きすぎるかを判定すること：

| 基準 | 閾値 |
|---|---|
| 行数 | 300行を超える |
| メソッド数 | 15個を超える |
| 責務の数 | 2つ以上の異なる関心事を持つ |

分割が必要な場合は責務ごとに分割したクラス名と役割を示す。
不要な場合は「分割不要：現状の行数・責務で問題なし」と明記する。

### 5-3. フォルダ構成の提案

分割後、機能が類似するクラスは同一フォルダに配置すること
（同じドメイン／同じレイヤー／呼び出し関係が強いクラス）。

```
src/main/java/com/example/order/
├── OrderService.java          ← 注文の登録・キャンセル
├── OrderQueryService.java     ← 注文の検索・一覧取得（分割後新設）
└── OrderRepository.java       ← DB操作（既存）
```

### 5-4. 出力フォーマット

```
## 改善版コード

### フォルダ構成
（5-3 の形式）

### [ファイル名1]
```java
（完全なコード。省略しない）
```

### [ファイル名2]
```java
（完全なコード。省略しない）
```

### 分割理由
（2〜3文）
```

---

# 禁止事項
- コードスタイルのみの指摘
- 「改善できます」だけの曖昧表現
- 根拠のない推測による指摘
- 影響範囲の未記載
- 改善版コードの省略・一部のみの出力（"..." で省略しない）
- 「コードが大きくなるため段階的に対応する」等の理由で
  改善版コードの生成自体を回避・延期すること
- 「まずP0/P1のみ修正し、その後対応する」という方針提示のみで
  Step5の出力を終えること

## Step5 は方針提案では完了しない

  NG: 「P0/P1から段階的に修正するのが妥当です」（で終わる）
  OK: P0/P1を全て修正した完全なコードをこの場で出力する

コードが長大になる場合は 5-2 のクラス分割で対応し、
分割した各ファイルのコードを全て提示すること。
「後で」「次回」「別途」という言葉で
コード生成を終わらせることは禁止する。

---

# 出力
ロードされた Template の形式に従い、Step1〜4 の分析結果を出力した後、
Step5 の改善版コードを必ず追加で出力すること。
